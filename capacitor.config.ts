import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yuton999.swimmingnote',
  appName: 'my swimming note',
  webDir: 'www'
};

export default config;
